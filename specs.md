# hotel-finder — spec moved

This monolithic spec has been reorganized into concern-focused files under
[`spec/`](spec/README.md), managed with [specflow](https://github.com/MatanKoby/specflow).

Start at **[`spec/README.md`](spec/README.md)** for the identity, file map, and reading order.
Forward-looking work (status, next steps, open questions) is in
[`spec/roadmap.md`](spec/roadmap.md); active batches are in [`BUILD_QUEUE.md`](BUILD_QUEUE.md).

Edit the spec via the procedure in `specflow/procedures/spec-edit.md` (edit the file matching
the concern; cross-reference rather than restate). This pointer can be deleted once nothing
references `specs.md`.
